<div class="columns is-mobile">
  <div class="column">
    <h2 class="title is-4"><%= configFunctions.getConfigProperty('equipment.sectionName') %></h2>
  </div>
  <div class="column is-narrow">
    <% if (isEdit) { %>
    <button class="button is-small is-success" id="button--addEquipment" type="button">
      <span class="icon"><i class="fa-solid fa-plus"></i></span>
      <span>Add <%= configFunctions.getConfigProperty('equipment.sectionNameSingular') %></span>
    </button>
    <% } %>
  </div>
</div>

<div id="container--equipment">
  <!-- Equipment will be loaded here -->
</div>
